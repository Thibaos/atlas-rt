# Vulkano ray tracing API and limits (git main, ticket 01)

> Research ticket: rendering-core ticket 01 (the scratch tracker is
> archived; the effort shipped as ADRs 0001–0006).
> Verification date: 2026-07-30 (vulkano master HEAD `fb4cfdb56`), source-verified against the vulkano repo at master and at atlas-rt's pinned commit `eae054666` (both fetched from GitHub raw at those exact SHAs), plus atlas-rt's `Cargo.lock` and `src/`.

## Executive answer

atlas-rt's exact vulkano API (`Cargo.lock` pins git commit `eae054666db41662dbbb94a6ab142e92ed9eda33`, "0.35.0", dated 2026-05-30) is **fully capable of the destination design**: procedural AABB BLASes (`AccelerationStructureGeometries::Aabbs` + `AabbPositions`), RT pipelines with procedural hit groups (intersection + closest-hit + any-hit), SBT, `trace_rays`, refit (`BuildAccelerationStructureMode::Update`, the KHR `vkCmdBuildAccelerationStructuresKHR` mode=UPDATE path), compaction, and taskgraph bindless (AS descriptors at global-set binding 4, `vko_accelerationStructureEXT`). But **current git main (HEAD `fb4cfdb56`, 2026-07-30) has drifted under atlas-rt: an unreleased, mid-flight breaking rework** changes exactly the AS-build structs atlas-rt uses (`AccelerationStructureGeometries` → `&[AccelerationStructureGeometry]`, scratch `Subbuffer` → `DeviceAddress`, `Update(Arc)` → `Update` + `src_acceleration_structure`) and removes `bind_pipeline_ray_tracing` (unified `bind_pipeline(Pipeline enum)`). **atlas-rt's `src/rt/acceleration_structure.rs` and `src/tasks/render.rs` compile against the pin today but will not compile against current main without a port.** Ray queries and opacity micromaps are reference-only: ray queries are shader-side only (vulkano exposes the `ray_query` feature flag, nothing CPU-side); `ext_opacity_micromap` feature flags exist but there is **no micromap API** in vulkano (build flags are commented-out TODOs), and OMM is triangle-only anyway.

## Capabilities matrix

Pinned = atlas-rt `Cargo.lock` commit `eae054666` (2026-05-30). Main = vulkano master `fb4cfdb56` (2026-07-30). Docs.rs `latest` = crates.io 0.35.2 (2025-08-16), which matches the _pinned-era_ API shape, **not** main. Do not use docs.rs as ground truth for main.

| #   | Capability                  | Pinned (atlas-rt today)                                                                                                                                                                                                                                                                                                                          | Current main                                                                                                                  | Notes                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | BLAS/TLAS creation          | `AccelerationStructure::new(&device, &AccelerationStructureCreateInfo { buffer, offset, size, ty })`; `device_address() -> NonZero<DeviceAddress>`                                                                                                                                                                                               | Same                                                                                                                          | Buffer usage `ACCELERATION_STRUCTURE_STORAGE` (+`SHADER_DEVICE_ADDRESS`); offset must be 256-aligned; size from `acceleration_structure_build_sizes` [1][2]                                                                                                                                                                                                                                                 |
| 2   | Geometry: Triangles         | `AccelerationStructureGeometries::Triangles(Vec<AccelerationStructureGeometryTrianglesData>)`; `vertex_data: Option<Subbuffer<[u8]>>`                                                                                                                                                                                                            | `&[AccelerationStructureGeometry]` wrapping `GeometryData::Triangles(TrianglesData { vertex_data: DeviceAddress, .. })`       | atlas-rt's `build_blas` is the pinned shape; on main vertex data is a device address [1][3]                                                                                                                                                                                                                                                                                                                 |
| 3   | Geometry: AABBs (exact API) | `AccelerationStructureGeometries::Aabbs(vec![AccelerationStructureGeometryAabbsData { flags, data: Some(aabb_subbuffer.into_bytes()), stride: size_of::<AabbPositions>() as u32, ..Default::default() }])`; element type `AabbPositions { min: [f32;3], max: [f32;3] }` (`#[repr(C)]`, Pod)                                                      | `AccelerationStructureGeometry::new(GeometryData::Aabbs(AabbsData { data: aabb_buffer.device_address().get(), stride, .. }))` | AABB buffer needs `ACCELERATION_STRUCTURE_BUILD_INPUT_READ_ONLY` (VUID 03673) + `SHADER_DEVICE_ADDRESS`; data address 8-aligned (03714), stride % 8 == 0 (03545), primitive_offset % 8 == 0 (03659); inactive AABB = `min[0] == NaN` [1][3]                                                                                                                                                                 |
| 4   | Geometry: Instances (TLAS)  | `AccelerationStructureGeometryInstancesData::new(AccelerationStructureGeometryInstancesDataType::Values(Some(subbuffer)))`; `AccelerationStructureInstance { transform, instance_custom_index_and_mask: Packed24_8, instance_shader_binding_table_record_offset_and_flags, acceleration_structure_reference }` (Pod)                             | `InstancesData { data: DeviceAddress, array_of_pointers: bool }`                                                              | instance data 16-aligned (03715); `max_instance_count` enforced at build (03801); material/visibility packing via 24-bit custom index + 8-bit mask = what atlas-rt uses for palette lookup [1][2]                                                                                                                                                                                                           |
| 5   | Build mode Build            | `BuildAccelerationStructureMode::Build`                                                                                                                                                                                                                                                                                                          | Same (unit variant)                                                                                                           | `vkCmdBuildAccelerationStructuresKHR` mode=BUILD [3]                                                                                                                                                                                                                                                                                                                                                        |
| 6   | Refit (Update)              | `BuildAccelerationStructureMode::Update(Arc<AccelerationStructure>)` (src AS carried in the enum)                                                                                                                                                                                                                                                | `Update` unit variant + new `src_acceleration_structure: Option<&Arc<AS>>` field                                              | **Refit IS exposed**. This is `vkCmdBuildAccelerationStructuresKHR` mode=UPDATE (the KHR refit path; `vkCmdUpdateAccelerationStructureKHR` is the legacy NV-only command, not used by vulkano). Requires `ALLOW_UPDATE` at initial build; may change buffers/strides/offsets but **not** geometry count/types or active/inactive status ⇒ voxel add/remove = rebuild (matches ticket 02 decision) [1][3][4] |
| 7   | Build flags                 | `ALLOW_UPDATE`, `ALLOW_COMPACTION`, `PREFER_FAST_TRACE`, `PREFER_FAST_BUILD`, `LOW_MEMORY`                                                                                                                                                                                                                                                       | Same                                                                                                                          | `PREFER_FAST_TRACE` \| `PREFER_FAST_BUILD` rejected together (VUID 03796). atlas-rt: TLAS = PREFER_FAST_TRACE \| ALLOW_UPDATE (ALLOW_UPDATE currently unused), BLAS = PREFER_FAST_TRACE [1][2]                                                                                                                                                                                                                     |
| 8   | Compaction                  | `CopyAccelerationStructureMode::Compact` + `copy_acceleration_structure` + `write_acceleration_structures_properties` query pool                                                                                                                                                                                                                 | Same                                                                                                                          | Needs `ALLOW_COMPACTION` at build; query pool (`QueryType::AccelerationStructureCompactedSize`) reads back sizes [1]                                                                                                                                                                                                                                                                                        |
| 9   | Scratch sizing / alignment  | `device.acceleration_structure_build_sizes(AccelerationStructureBuildType::Device, &info, &[primitive_count])` → `{ build_scratch_size, acceleration_structure_size, update_scratch_size }`; scratch as `Subbuffer<[u8]>` with `SHADER_DEVICE_ADDRESS`+`STORAGE_BUFFER`                                                                          | Same sizing call; scratch is a `DeviceAddress`                                                                                | Scratch device address must be a multiple of `min_acceleration_structure_scratch_offset_alignment` (VUID 03710). The main-branch example allocates scratch aligned to that property; atlas-rt's pinned code relies on the allocator (works, but align explicitly when porting) [1][3][5]                                                                                                                    |
| 10  | Device-address plumbing     | `BufferUsage::SHADER_DEVICE_ADDRESS`; `blas.device_address().into()` → `AccelerationStructureInstance::acceleration_structure_reference`; data buffers handed as `Subbuffer`                                                                                                                                                                     | Data handed as `.device_address().get()`                                                                                      | atlas-rt already does the instance-reference part; main pushes addresses down to vertex/AABB/instance data too [1][3]                                                                                                                                                                                                                                                                                       |
| 11  | RayTracingPipeline          | `RayTracingPipeline::new(&device, None, &RayTracingPipelineCreateInfo { stages: &[...], groups: &[...], layout: &Arc<PipelineLayout>, max_pipeline_ray_recursion_depth, .. })`                                                                                                                                                                   | Same shape (fields now slices of `PipelineShaderStageCreateInfo<'a>`)                                                         | `Pipeline` trait → enum on main; `RayTracingPipeline::new` still takes cache as `Option<&Arc<PipelineCache>>`; deferred-op param is hardcoded null (TODO). No deferred RT pipeline build [2][6]                                                                                                                                                                                                             |
| 12  | Shader groups               | `RayTracingShaderGroupCreateInfo::{ General { general_shader }, ProceduralHit { closest_hit_shader, any_hit_shader, intersection_shader }, TrianglesHit { closest_hit_shader, any_hit_shader } }`                                                                                                                                                | Same                                                                                                                          | Execution models validated per group (raygen/miss/callable; intersection; any-hit/closest-hit). atlas-rt pipeline = raygen(0), miss(1), ProceduralHit{intersection:2, closest_hit:3, any_hit:None} [2][6]                                                                                                                                                                                                   |
| 13  | ShaderBindingTable          | `ShaderBindingTable::new(&allocator, &pipeline)`; `.addresses()` → `ShaderBindingTableAddresses { raygen, miss, hit, callable: StridedDeviceAddressRegion }`                                                                                                                                                                                     | Same                                                                                                                          | Layout: raygen stride = handle_size aligned to `shader_group_base_alignment`; miss/hit/callable stride = handle_size aligned to `shader_group_handle_alignment`; buffer usages TRANSFER_SRC \| SHADER_DEVICE_ADDRESS \| SHADER_BINDING_TABLE. **No per-record user-data API**: the helper packs only handles into a private buffer. Per-hit data must ride the instance custom index (atlas-rt today), the instance SBT record offset (24-bit field), or a hand-built SBT whose addresses you feed to `trace_rays` [2][6] |
| 14  | trace_rays                  | taskgraph `RecordingCommandBuffer::trace_rays(&ShaderBindingTableAddresses, [u32; 3])`                                                                                                                                                                                                                                                           | Same + new `trace_rays_indirect` (feature `ray_tracing_pipeline_trace_rays_indirect`, added in PR #2813, merged 2026-06)      | atlas-rt already calls it per frame [7][8]                                                                                                                                                                                                                                                                                                                                                                  |
| 15  | Recursion depth             | `max_pipeline_ray_recursion_depth` default 1, validated against `properties().max_ray_recursion_depth` (VUID 03589)                                                                                                                                                                                                                              | Same                                                                                                                          | atlas-rt uses 1 (primary visibility only) [2]                                                                                                                                                                                                                                                                                                                                                               |
| 16  | Push constants              | `cbf.push_constants(layout, 0, &raygen::PushConstants { image_id, acceleration_structure_id, camera_buffer_id, ... })`                                                                                                                                                                                                                           | Same signature (`layout: &Arc<PipelineLayout>, offset, &impl BufferContents`)                                                 | Ranges derived from all stages incl. RT stages via `push_constant_ranges_from_stages`; atlas-rt packs bindless IDs (needs `storage_push_constant8`) [7][9]                                                                                                                                                                                                                                                  |
| 17  | Ray queries (reference)     | Feature flag `DeviceFeatures::ray_query` (requires `khr_ray_query`); shader-side only (`GL_EXT_ray_query` / `OpRayQueryProceedKHR`). No CPU-side API exists in Vulkan, so nothing else to expose                                                                                                                                                 | Same                                                                                                                          | No ray-query example on main (only `examples/ray-tracing`, triangle-based); the historical ray-query example (PR #2314) is gone from the current tree. atlas-rt doesn't need it [10][11]                                                                                                                                                                                                                    |
| 18  | Limits                      | `device.physical_device().properties().max_instance_count.unwrap()` (atlas-rt: `/ 8`, dbg! at startup)                                                                                                                                                                                                                                           | Same accessor; fields still `Option<u32>`                                                                                     | Related fields: `max_geometry_count`, `max_primitive_count` (triangles _and_ AABBs), `max_ray_recursion_depth`, `shader_group_handle_size/alignment`, `shader_group_base_alignment`, `min_acceleration_structure_scratch_offset_alignment`. Builds validated against them (VUIDs 03793/03794/03795/03801) [1][3][5]                                                                                         |
| 19  | Opacity micromaps           | Extension/feature flags exist (`DeviceExtensions::ext_opacity_micromap`, `micromap`, `micromap_capture_replay`, `micromap_host_commands`) but **no micromap API**: `ALLOW_OPACITY_MICROMAP_*` build flags and `FORCE_OPACITY_MICROMAP_2_STATE`/`DISABLE_OPACITY_MICROMAPS` instance flags are commented-out TODOs in `acceleration_structure.rs` | Same (still TODO)                                                                                                             | OMM is sub-triangle opacity for triangle geometry only and a BLAS cannot mix AABBs and triangles ⇒ irrelevant to the AABB-voxel destination (confirmed, ticket 02 agreement) [1][12]                                                                                                                                                                                                                        |
| 20  | Bindless (taskgraph)        | `BindlessContext` global set bindings: 0 sampler, 1 sampled image, 2 storage image, 3 storage buffer, 4 acceleration structure (AS binding added only when `acceleration_structure` feature enabled); `GlobalDescriptorSet::{ add_acceleration_structure, create_storage_buffer, add_storage_image }`; `pipeline_layout_from_stages`             | Same                                                                                                                          | atlas-rt already uses this path (`bcx.global_set().add_acceleration_structure(...)`, `create_storage_buffer`, push-constant IDs); bindings must match vulkano.glsl constants (comment cross-checked in both files) [9][13]                                                                                                                                                                                  |
| 21  | vulkano.glsl RT             | `AccelerationStructureId` struct + `VKO_ACCELERATION_STRUCTURE_BINDING 4` + `#if VKO_ACCELERATION_STRUCTURE_ENABLED ... uniform accelerationStructureEXT _vko_acceleration_structures_accelerationStructureEXT[];` + `vko_accelerationStructureEXT(id)` macro; `VKO_DECLARE_STORAGE_BUFFER`/`VKO_DECLARE_STORAGE_IMAGE`                          | Same                                                                                                                          | Header at `vulkano-shaders/include/vulkano.glsl`; GLSL side needs `GL_EXT_ray_query` or `GL_EXT_ray_tracing` enabled in the shader [14]                                                                                                                                                                                                                                                                     |
| 22  | Deferred host operations    | `vulkano::deferred::DeferredOperation` exists (join/result/wait/max_concurrency); `khr_deferred_host_operations` extension required                                                                                                                                                                                                              | Same                                                                                                                          | **Not wired into RT pipeline creation**. `RayTracingPipeline::new_unchecked` passes `vk::DeferredOperationKHR::null()` with a TODO; the extension atlas-rt enables is only used for the `DeferredOperation` object itself [2][15]                                                                                                                                                                           |
| 23  | Version facts               | Pin `eae054666` (2026-05-30, "0.35.0" git); crates.io latest 0.35.2 (2025-08-16, no release since)                                                                                                                                                                                                                                               | HEAD `fb4cfdb56` (2026-07-30)                                                                                                 | Main has an **unreleased breaking rework**: MSRV 1.88; shaderc removed (glslc must be on PATH); `Pipeline` trait → enum; single `bind_pipeline`; `PipelineDescriptorSetLayoutCreateInfo` → `PipelineLayout::from_stages`; AS API → device-address/slice based; `try_` + panicking-variant split [4][16]                                                                                                     |

## Findings

1. **atlas-rt's pin is ~2 months behind main, and the gap is a breaking rework, not patches.** `Cargo.lock` pins all four vulkano crates to git commit `eae054666db41662dbbb94a6ab142e92ed9eda33` ("Fix BuddyAllocator not taking the region's offset into account (#2809)", authored 2026-05-30); master HEAD is `fb4cfdb56998f16b0a1265b813efbec63b93c036` ("Optimize the suballocation lists (#2825)", 2026-07-30). Between them, the unreleased changelog (MSRV 1.88, `Pipeline` enum, `bind_pipeline` unification, `PipelineLayout::from_stages`, AS API rework, glslc-required) landed. [Cargo.lock (atlas-rt)](Cargo.lock), [commit eae0546](https://api.github.com/repos/vulkano-rs/vulkano/commits/eae054666db41662dbbb94a6ab142e92ed9eda33), [commit fb4cfdb](https://api.github.com/repos/vulkano-rs/vulkano/commits/fb4cfdb56998f16b0a1265b813efbec63b93c036), [CHANGELOG.md (master)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/CHANGELOG.md)

2. **What breaks on main, file by file in atlas-rt.** `src/rt/acceleration_structure.rs` uses `AccelerationStructureGeometries::Triangles(vec![...])` (→ `geometries: &[AccelerationStructureGeometry<'a>]`), `scratch_data: Some(scratch_buffer)` (→ `scratch_data: DeviceAddress`), `dst_acceleration_structure: Some(acceleration.clone())` (→ `Option<&Arc<...>>`), `BuildAccelerationStructureMode` (Update variant loses its payload; source AS moves to `src_acceleration_structure`), `AccelerationStructureGeometryTrianglesData::new(Format)` with `vertex_data: Some(...)` (→ device addresses + `new()`), and the TLAS `AccelerationStructureGeometryInstancesDataType::Values(...)` (→ `data: DeviceAddress` + `array_of_pointers: bool`). `src/tasks/render.rs` calls `cbf.bind_pipeline_ray_tracing(&self.pipeline)`, which no longer exists on main (taskgraph now has only `bind_pipeline(impl Into<Pipeline>)`, with a `Pipeline::RayTracing` variant). `trace_rays`, `push_constants`, `ShaderBindingTable`, `RayTracingPipelineCreateInfo`, `RayTracingShaderGroupCreateInfo` and the taskgraph bindless calls are **unchanged in shape** and compile as-is. Source-verified at both SHAs. [pinned acceleration_structure.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/acceleration_structure.rs), [master acceleration_structure.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/acceleration_structure.rs), [pinned taskgraph pipeline.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano-taskgraph/src/command_buffer/commands/pipeline.rs), [master taskgraph bind_push.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano-taskgraph/src/command_buffer/commands/bind_push.rs), [CHANGELOG.md](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/CHANGELOG.md)

3. **Procedural AABB geometry, exact API, both eras.** Pinned: `AccelerationStructureGeometries::Aabbs(vec![AccelerationStructureGeometryAabbsData { flags: GeometryFlags::empty(), data: Some(aabb_subbuffer.into_bytes()), stride: size_of::<AabbPositions>() as u32, ..Default::default() }])`, where the data subbuffer is `Subbuffer<[AabbPositions]>`, `AabbPositions { min: [f32; 3], max: [f32; 3] }` is `#[repr(C)]` `Pod`/`Zeroable`, and the buffer must carry `ACCELERATION_STRUCTURE_BUILD_INPUT_READ_ONLY` (VUID-vkCmdBuildAccelerationStructuresKHR-geometry-03673) plus `SHADER_DEVICE_ADDRESS`. Validated: data device address multiple of 8 (03714), `stride` multiple of 8 (VUID 03545), `primitive_offset` multiple of 8 (03659), bounds vs buffer size. Inactive AABB = `min[0]` is NaN. Main: same structs minus the geometry enum. `AccelerationStructureGeometry::new(AccelerationStructureGeometryData::Aabbs(...))` takes `data: buffer.device_address().get()`. [pinned acceleration_structure.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/acceleration_structure.rs), [master acceleration_structure.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/acceleration_structure.rs), [pinned command validation](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/command_buffer/commands/acceleration_structure.rs)

4. **Refit is exposed, under the KHR build command.** `BuildAccelerationStructureMode::Update` maps to `vkCmdBuildAccelerationStructuresKHR` with `mode = VK_BUILD_ACCELERATION_STRUCTURE_MODE_UPDATE_KHR`, which _is_ the KHR refit path (`vkCmdUpdateAccelerationStructureKHR` is the deprecated NV-only command; vulkano doesn't wrap NV RT). Constraints documented in vulkano: the update may change scratch, vertex/AABB/instance data buffers, strides and offsets, but must keep the same geometry variant/count, same `primitive_count`, same index values, and may not flip active/inactive items; the dst must have been built with `ALLOW_UPDATE` (in-place update allowed, src == dst). Consequence for atlas-rt: value-only changes (camera-driven TLAS rebuilds where instance transforms change) can refit; voxel add/remove changes counts ⇒ rebuild (ticket 02's conclusion holds). [pinned acceleration_structure.rs module docs](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/acceleration_structure.rs), [master module docs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/acceleration_structure.rs), [VkCmdBuildAccelerationStructuresKHR refpage](https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBuildAccelerationStructuresKHR.html)

5. **Limits are queried through `physical_device().properties()`, and builds are validated against them.** atlas-rt today: `self.device.physical_device().properties().max_instance_count.unwrap() / 8` then `dbg!` (src/app.rs, verified); ~2M instances on a 2²⁴ driver per the map. Same accessor on main. Fields are `Option<u32>`-style (`unwrap` in atlas-rt and upstream examples). vulkano also validates at build time: `max_geometry_count` (03793), `max_primitive_count` for AABBs/triangles (03794/03795), `max_instance_count` for TLAS (03801), scratch alignment (03710), plus alignment VUIDs above. [atlas-rt src/app.rs](src/app.rs), [pinned command validation](https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/command_buffer/commands/acceleration_structure.rs), [master command validation](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/command_buffer/commands/acceleration_structure.rs)

6. **Ray queries: shader-side only, nothing to expose CPU-side.** Vulkan has no host-side ray-query API; `VK_KHR_ray_query` is entirely `OpRayQueryProceedKHR` in shaders. vulkano's API is `DeviceFeatures::ray_query` (requires `DeviceExtensions::khr_ray_query`) + vulkano-shaders compiling `GL_EXT_ray_query` + the same bindless AS descriptor path. Current main ships only a triangle-based `examples/ray-tracing` (no ray-query example). Confirmed feature flag in `vulkano/autogen-out/features.rs` on master. atlas-rt doesn't need it. Record it as reference/alternative only. [master features.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/autogen-out/features.rs), [VK_KHR_ray_query refpage](https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_ray_query.html), [Vulkan ray tracing guide](https://docs.vulkan.org/guide/latest/extensions/ray_tracing.html), [examples tree](https://api.github.com/repos/vulkano-rs/vulkano/git/trees/master?recursive=1)

7. **Opacity micromaps: extension flags exist, API does not.** `DeviceExtensions::ext_opacity_micromap` and `DeviceFeatures::{micromap, micromap_capture_replay, micromap_host_commands}` are auto-generated from vk.xml, but `acceleration_structure.rs` (pinned _and_ main) has `ALLOW_OPACITY_MICROMAP_UPDATE`, `ALLOW_DISABLE_OPACITY_MICROMAPS`, `ALLOW_OPACITY_MICROMAP_DATA_UPDATE` build flags and `FORCE_OPACITY_MICROMAP_2_STATE`/`DISABLE_OPACITY_MICROMAPS` instance flags commented out with `/* TODO: enable */`; there is no `OpacityMicromap` type anywhere in the tree. Even if it landed, OMM is sub-triangle opacity for triangle geometry and a BLAS can't mix AABBs and triangles, so this is irrelevant to AABB voxels (ticket 02's agreement confirmed). [master acceleration_structure.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/acceleration_structure.rs), [master features.rs](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/autogen-out/features.rs), [VK_EXT_opacity_micromap refpage](https://docs.vulkan.org/refpages/latest/refpages/source/VK_EXT_opacity_micromap.html)

8. **Bindless + vulkano.glsl RT support is the same on both eras and is exactly what atlas-rt uses.** Taskgraph global set: bindings 0–3 sampler/sampled image/storage image/storage buffer, binding 4 acceleration structures, added only when the `acceleration_structure` feature is enabled, written via `WriteDescriptorSet::acceleration_structure_array` with UPDATE_AFTER_BIND|PARTIALLY_BOUND flags (requires `descriptor_binding_acceleration_structure_update_after_bind`, which atlas-rt enables). `vulkano-shaders/include/vulkano.glsl` mirrors the constants (`VKO_ACCELERATION_STRUCTURE_BINDING 4`) and provides `vko_accelerationStructureEXT(id)` behind `VKO_ACCELERATION_STRUCTURE_ENABLED` plus `VKO_DECLARE_STORAGE_BUFFER`/`VKO_DECLARE_STORAGE_IMAGE`. The header comment says both sides must stay in sync. Push constants carry `StorageImageId`/`AccelerationStructureId`/`StorageBufferId`; ranges come from all pipeline stages via `push_constant_ranges_from_stages`. [taskgraph descriptor_set.rs (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano-taskgraph/src/descriptor_set.rs), [vulkano.glsl (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano-shaders/include/vulkano.glsl), [atlas-rt src/tasks/render.rs](src/tasks/render.rs)

9. **SBT per-hit data: the built-in helper is handle-only; plan for instance/record data or a hand-built SBT.** `ShaderBindingTable::new` allocates one contiguous buffer (usages TRANSFER_SRC|SHADER_DEVICE_ADDRESS|SHADER_BINDING_TABLE, HOST_SEQUENTIAL_WRITE|PREFER_DEVICE) and writes only group handles (raygen stride = handle size aligned to `shader_group_base_alignment`; miss/hit/callable stride = handle size aligned to `shader_group_handle_alignment`; sizes 16-byte/alignment padded). The buffer is private (`_buffer` field). There is no API to append per-record user data. Options for the destination: (a) keep atlas-rt's instance-custom-index transport for per-hit data (24-bit index, 8-bit mask, read via `gl_InstanceCustomIndexEXT`); (b) use `instance_shader_binding_table_record_offset_and_flags` (24-bit record offset) to select hit records; (c) build a custom SBT buffer and pass your own `ShaderBindingTableAddresses` (the type is public; `trace_rays` takes addresses, not the helper). [pipeline/ray_tracing.rs (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/pipeline/ray_tracing.rs)

10. **Known bugs / validation gotchas (vulkano-side).** (a) vulkano#2703 "Acceleration structure alignment issue" (open, 2025-06): AS memory alignment regressions around 0.34.1/0.35.1, relevant to the pinned era. Align AS/scratch to `min_acceleration_structure_scratch_offset_alignment` explicitly when porting (the main example does this). (b) vulkano#2709 "single threaded data race during acceleration structure construction" (open, wontfix/old sync, 2025-07): spurious "OneTimeSubmit command buffer submitted twice" panics during AS builds. atlas-rt's `vulkano_taskgraph::execute` + `wait_idle` pattern is the documented workaround. (c) `PREFER_FAST_TRACE`|`PREFER_FAST_BUILD` together → ValidationError (03796). (d) On main, `AccelerationStructureBuildGeometryInfo::ty` must be set explicitly (Generic is rejected, VUID 03654). The pinned-era struct derived its type from the geometry variant, so this is a port-time trap. (e) Validation-layer gap: VVL does not flag building a TLAS and its referenced BLAS in the same `vkCmdBuildAccelerationStructuresKHR` call (KhronosGroup/Vulkan-ValidationLayers#11078). Keep TLAS builds depending on BLAS builds in separate submissions/flights as atlas-rt already does. (f) Fixed on main: a bindless `DescriptorBindingRequirements` validation panic (the changelog quotes "When using bindless, the validation checks ... would panic"). [issue #2703](https://github.com/vulkano-rs/vulkano/issues/2703), [issue #2709](https://github.com/vulkano-rs/vulkano/issues/2709), [VVL #11078](https://github.com/KhronosGroup/Vulkan-ValidationLayers/issues/11078), [CHANGELOG.md](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/CHANGELOG.md)

11. **Deferred host operations exist but are not used for RT pipeline creation.** `vulkano::deferred::DeferredOperation` (create/join/result/wait/max_concurrency, requires `khr_deferred_host_operations`) is present on main, but `RayTracingPipeline::new_unchecked` passes `vk::DeferredOperationKHR::null()` with a `// TODO: RayTracing: deferred_operation`. Pipeline creation is always synchronous. atlas-rt enables the extension today; that's fine, it just buys nothing until vulkano wires it in. [deferred.rs (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/deferred.rs), [ray_tracing.rs (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/pipeline/ray_tracing.rs)

12. **Upstream example on main matches atlas-rt's architecture 1:1.** `examples/ray-tracing/` (main) does taskgraph + `vulkano_taskgraph::execute` for AS builds (with `cbf.as_raw().build_acceleration_structure(&info, slice::from_ref(&range))`), `bcx.global_set().add_acceleration_structure(tlas)`, `create_storage_buffer`, `push_constants` with bindless IDs, `bind_pipeline`, `trace_rays`, and keeps the BLAS alive (`_blas` field) because the TLAS references it, the same pattern as `src/tasks/render.rs`. It is triangle-based (no AABB example exists anywhere in the repo), so the AABB path must be validated by atlas-rt's own port. [examples/ray-tracing/scene.rs (main)](https://raw.githubusercontent.com/vulkano-rs/vulkano/master/examples/ray-tracing/scene.rs)

## Sources

- Kept:
  - vulkano CHANGELOG.md (master, raw), authoritative list of the unreleased breaking rework (MSRV 1.88, Pipeline enum, glslc, AS/scratch changes, fixes). https://raw.githubusercontent.com/vulkano-rs/vulkano/master/CHANGELOG.md
  - vulkano/src/acceleration_structure.rs at master AND at `eae054666`, primary API evidence for both eras (AabbsData/AabbPositions/geometries enum vs slice/DeviceAddress, modes, flags, update constraints, TODO'd micromap flags). https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/acceleration_structure.rs
  - vulkano/src/pipeline/ray_tracing.rs (master), RayTracingPipeline/SBT/addresses/groups/recursion validation, deferred-op null. https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/src/pipeline/ray_tracing.rs
  - vulkano/src/command_buffer/commands/acceleration_structure.rs at master and pinned, build command signatures + VUID-verified limit/alignment validation. https://raw.githubusercontent.com/vulkano-rs/vulkano/eae054666db41662dbbb94a6ab142e92ed9eda33/vulkano/src/command_buffer/commands/acceleration_structure.rs
  - vulkano-taskgraph (master + pinned): descriptor_set.rs (global-set bindings incl. AS binding 4), command_buffer/commands/{pipeline,bind_push}.rs (trace_rays, unified bind_pipeline). https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano-taskgraph/src/descriptor_set.rs
  - vulkano-shaders/include/vulkano.glsl (master), AccelerationStructureId/vko_accelerationStructureEXT/binding constants. https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano-shaders/include/vulkano.glsl
  - vulkano/autogen-out/features.rs (master), feature-flag names (ray_query, ray_tracing_pipeline, trace_rays_indirect[2], micromap...). https://raw.githubusercontent.com/vulkano-rs/vulkano/master/vulkano/autogen-out/features.rs
  - examples/ray-tracing/ (master), executable reference for the whole taskgraph+RT flow on main. https://raw.githubusercontent.com/vulkano-rs/vulkano/master/examples/ray-tracing/scene.rs
  - GitHub commits API for `fb4cfdb56` and `eae054666`, commit dates/messages. https://api.github.com/repos/vulkano-rs/vulkano/commits/master
  - atlas-rt Cargo.lock, src/app.rs, src/rt/acceleration_structure.rs, src/tasks/render.rs, local primary evidence for the pin and current usage.
  - PR #2813 (trace_rays_indirect support), https://github.com/vulkano-rs/vulkano/pull/2813
  - vulkano issues #2703, #2709, known RT bugs. https://github.com/vulkano-rs/vulkano/issues/2703
  - Khronos refpages: vkCmdBuildAccelerationStructuresKHR (mode=UPDATE/refit semantics, VUIDs), VK_KHR_ray_query, VK_EXT_opacity_micromap, https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBuildAccelerationStructuresKHR.html
  - VVL issue #11078 (TLAS+BLAS in one command not checked), https://github.com/KhronosGroup/Vulkan-ValidationLayers/issues/11078
- Dropped:
  - docs.rs/vulkano/latest, used only for _drift illustration_. It documents crates.io 0.35.2 (matches the pinned-era API, e.g. SmallVec-typed build_range_infos and `bind_pipeline_ray_tracing`), NOT git main. Flagged in the matrix; not evidence for main.
  - gfx-rs/wgpu issues #8743/#8131 (AS scratch alignment, RT validation), different crate (wgpu-rt out of scope). Kept only as corroboration that scratch-alignment bugs are ecosystem-wide.
  - NVIDIA forum SBT thread and generic Khronos RT tutorials, background only, no vulkano-specific claims.
  - vulkano issue #2455 (original VK_KHR_ray_tracing_pipeline support request, 2021), historical. Superseded by the shipped implementation.

## Gaps

- **No upstream AABB/procedural example or test exists** in the vulkano repo (both eras). The exact `AabbPositions` packing, inactive-AABB NaN convention, and the procedural hit path are documented in source and the Khronos spec but not exercised by any vulkano example. atlas-rt's AABB port is the first consumer and should be validated with the Vulkan SDK layers + NSight/layer RT validation.
- **Main is mid-rework**: commit-to-commit API churn is ongoing (the unreleased changelog's "Global changes" are still landing). Any main-specific signature in this doc was verified at `fb4cfdb56` (2026-07-30) and may move before the next release; re-verify at port time.
- **SBT record layout with user data**: the built-in `ShaderBindingTable` is handle-only; the exact `shader_group_handle_alignment`/`shader_group_base_alignment` values on the RTX 3070 are device-dependent and were not measured here (owner's `dbg!` run or a one-line probe is needed if a custom SBT is chosen).
- **Perf numbers (build/refit cost, scratch sizes at atlas-rt's chunk counts)** are outside this API ticket; ticket 02/06 cover the cost model.
- `DeviceFeatures::ray_query` etc. were confirmed by name from autogen; no compile-test against main was possible in this environment (no git, no cargo build run). The atlas-rt breakage list is from reading both SHAs' sources, not from a compiler.

## Suggested next steps

1. When the port lands: keep the pin, add a `cargo update` + port task, or pin to a released 0.36-equivalent once one ships. Decide in the implementation ticket, not here.
2. Validate the AABB BLAS + procedural hit group on the RTX 3070 with layers enabled before wiring the destination's per-region BLAS plan.
